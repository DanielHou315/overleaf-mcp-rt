# Overleaf CE 3.x: SHARELATEX_* names and a standalone Mongo. 3.x needs no replica set, and one of its
# migrations reads from a secondary — which a single-node replica set doesn't have, so it times out.
SHARELATEX_APP_NAME=overleaf-mcp-rt live test (throw-away)
SHARELATEX_MONGO_URL=mongodb://mongo/sharelatex
SHARELATEX_REDIS_HOST=redis
REDIS_HOST=redis
SHARELATEX_SITE_URL=http://sharelatex
ENABLED_LINKED_FILE_TYPES=
ENABLE_CONVERSIONS=true
EMAIL_CONFIRMATION_DISABLED=true
